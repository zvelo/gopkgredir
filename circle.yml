machine:
  services:
    - docker

test:
  override:
    - CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -a -v -tags netgo -installsuffix netgo
    - docker build -t $DOCKER_IMAGE .

deployment:
  hub:
    branch: master
    commands:
      - docker push $(DOCKER_IMAGE):latest
